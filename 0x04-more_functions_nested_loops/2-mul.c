#include "main.h"

/**
 * mul - multiply two integers.
 * @a: The first number to multiply.
 * @b: The second number to multiply with first number.
 * Return: The result of mulplication.
 */

int mul(int a, int b)
{
	return (a * b);

}
